colang_version: "1.0"

models:
  - type: main
    engine: openai
    model: gemini-primary
    api_key_env_var: LITELLM_MASTER_KEY
    parameters:
      base_url: http://litellm:4000/v1

rails:
  input:
    flows:
      - check other tenant data
